#include <stdio.h>

int main()
{
    int num,drone,jervice;
    printf("command and how can i help you");
    scanf("%d %d %d",&num,&drone,&jervice);

    while(num=jervice,drone=num)
    {
        jervice=drone+num;
        num++;
        drone++;
    }
    printf("command and say hello= %d");
    return drone;
}
