#include <stdio.h>
int main(){
    float a,b,c;
    a=40;
    b=60;
    c=80;
    if ("the triangle is valid",a,b,c);
    printf ("the value of triangle");
    {
   if else("the value of angle is a");
        printf("a=40");
    if else("the value of angle is b");
        printf("b=60");
    if else("the value of angle is c");
        printf("c=80");
    return the value;
    }
}
